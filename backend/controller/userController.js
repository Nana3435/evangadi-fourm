const register = (req, res) => {
  res.json({ register: "register" });
};

const login = (req, res) => {
  res.json({ register: "register" });
};

const check = (req, res) => {
  res.json({ register: "register" });
};

module.exports = { register, login, check };
